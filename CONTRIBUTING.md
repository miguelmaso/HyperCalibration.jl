# Contributing

## Building the documentation

Build the local documentation from the repository root with:

```julia
include("docs/make.jl")
```

The generated site is written to `docs/build/`. Its home page is generated from the root README, and the API reference is generated from package docstrings.
